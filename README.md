# Sock analyst project 
This is a project for monitoring and analyzing network security events. It includes an agent that collects logs from various sources and sends them to a central server for analysis.
## Agent
The agent is responsible for collecting logs from specified sources and sending them to the server. It is configured using a YAML file.
### Configuration
The agent configuration is stored in `config.yaml`. It specifies the log sources to monitor.